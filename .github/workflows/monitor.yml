name: AAA WORKFLOW NUOVO (test)

on:
  workflow_dispatch: {}

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: |
          echo "QUESTO È IL WORKFLOW NUOVO"
          echo "REF: $GITHUB_WORKFLOW_REF"
